[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # Line length handled separately

[tool.ruff.lint.per-file-ignores]
"tools/rebuild_profile.py" = ["E402"]  # sys.path must precede package imports
