# Copy to .env and fill in your values. .env is gitignored.

# Video directories (mounted read-only into the container)
VIDEO_DIR_D=D:\Shows
VIDEO_DIR_S=S:\Shows

# HuggingFace token (needs pyannote/speaker-diarization-3.1 access)
HUGGINGFACE_TOKEN=hf_your_token_here
